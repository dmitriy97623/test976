#pragma once

// Подключение заголовочного файла модуля hardware для доступа к датчикам и состоянию
#include "hardware.h"

// --- Константы для работы ---
// Количество доступных диапазонов измерения давления
extern const int RANGES_COUNT;
// Массив максимальных значений для каждого диапазона (в единицах измерения)
extern const float RANGES[];
// Массив названий единиц измерения
textern const char* UNIT_NAMES[];
// Количество доступных единиц измерения
extern const int UNITS_COUNT;

// --- Глобальные переменные ---
// Текущий выбранный индекс диапазона
extern int currentRangeIndex;
// Текущий выбранный индекс единицы измерения
extern int currentUnitIndex;
// Нижняя уставка давления
extern float setpointLow;
// Верхняя уставка давления
extern float setpointHigh;
// Значение гистерезиса (зона нечувствительности)
extern float hysteresis;
// Калибровочное значение АЦП для 4 мА (минимум)
extern int calMin;
// Калибровочное значение АЦП для 20 мА (максимум)
extern int calMax;

// --- Функции инициализации ---
// Инициализация модуля логики (в данном случае не требуется)
void logicSetup();

// --- Основные функции ---
// Преобразование сырого сигнала АЦП в значение давления с учётом калибровки
// Выход: значение давления в текущих единицах измерения
tfloat readPressure();

// Управление состоянием реле на основе текущего давления и заданных уставок
// Вход: pressure - текущее значение давления
void controlRelays(float pressure);